{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-workflow-runner-ingress
  labels:
    app: workflow-runner
spec:
  podSelector:
    matchLabels:
      app: workflow-runner
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 8080
          protocol: TCP
{{- if .Values.networkPolicy.egress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-workflow-runner-egress
  labels:
    app: workflow-runner
spec:
  podSelector:
    matchLabels:
      app: workflow-runner
  policyTypes:
    - Egress
  egress:
    # Temporal Server
    - ports:
        - port: 7233
          protocol: TCP
    # OpenShell gateway (control plane + gateway-mediated HTTP routing to
    # sandboxes, issue #198 -- OpenShellSpawner never talks to sandbox pods
    # directly, everything is gateway-mediated)
    - ports:
        - port: 17670
          protocol: TCP
    # Sandbox pods (retained for deployments where the gateway's own
    # compute driver exposes a direct pod route rather than proxying)
    - ports:
        - port: 8080
          protocol: TCP
    # K8s API (TokenReview auth, runtime/auth.py)
    - ports:
        - port: 443
          protocol: TCP
    # DNS
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-sandbox-egress
  labels:
    app: workflow-runner
spec:
  podSelector:
    matchLabels:
      spawned-by: workflow-runner
  policyTypes:
    - Egress
  egress:
    # DNS
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    # LLM provider endpoints
    {{- range .Values.networkPolicy.egress.llmCidrs }}
    - to:
        - ipBlock:
            cidr: {{ . }}
      ports:
        - port: 443
          protocol: TCP
    {{- end }}
{{- end }}
{{- end }}
